<aside class="w-64 bg-white shadow-lg min-h-screen p-4">
  <nav class="space-y-2">
    <%= link_to "Dashboard", dashboard_path,
        class: "block px-4 py-2 rounded-md font-medium #{controller_name == 'dashboard' ? 'bg-blue-100 text-blue-800' : 'text-gray-700 hover:bg-gray-100'}" %>
    <% if current_organization %>
      <%= link_to "Clientes", clients_path,
          class: "block px-4 py-2 rounded-md font-medium #{controller_name == 'clients' ? 'bg-blue-100 text-blue-800' : 'text-gray-700 hover:bg-gray-100'}" %>
    <% end %>
  </nav>
</aside>
